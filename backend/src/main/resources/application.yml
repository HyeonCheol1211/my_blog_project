spring:
  datasource:
    # 'db' 대신 'localhost'를 사용해야 인텔리제이에서 접속 가능합니다.
    url: jdbc:mysql://localhost:3306/blog_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: "1234" # docker-compose에 적으신 비밀번호
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true