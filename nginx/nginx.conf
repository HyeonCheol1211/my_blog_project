server {
    listen 80;
    server_name hc-domain.site;

    # Certbot 인증용 경로 (이건 리다이렉트 예외로 두는 게 안전합니다)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # 그 외 모든 요청은 HTTPS로 던지기!
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name hc-domain.site;

    ssl_certificate /etc/letsencrypt/live/hc-domain.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/hc-domain.site/privkey.pem;

    location / {
        proxy_pass http://backend:8080;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
    }
}
